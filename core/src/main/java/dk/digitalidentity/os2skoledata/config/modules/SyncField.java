package dk.digitalidentity.os2skoledata.config.modules;

public enum SyncField {
	UNI_LOGIN,
	GROUP_IDS,
	NAME_PROTECTED,

	// only if name protected
	ALIAS_FIRST_NAME,

	// only if name protected
	ALIAS_FAMILY_NAME
}
